# Test Scrum-for-Developers infrastructure setup with Vagrant

To create a new vagrant vm and provision with ansible (in local mode), just execute
```
vagrant up
```

To re-provision an existing machine, execute
```
vagrant provision
```
